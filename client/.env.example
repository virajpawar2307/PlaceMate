# Public API base URL used by the frontend.
# For local development with Vite proxy, this can stay as /api.
# For production, set it to your deployed backend URL + /api.
# Example for current deployment:
# VITE_API_BASE_URL=https://placemate-26ej.onrender.com/api
VITE_API_BASE_URL=/api
